# Ignore everything by default
*
!*/

# Explicitly tracked
!CLAUDE.md
!settings.json
!skills/**
!rules/**
!.gitignore
!claude_desktop_config.json
!statusline.sh

# Machine-specific journal config (absolute path) — keep local, do not sync
claude/skills/journal/config.json
